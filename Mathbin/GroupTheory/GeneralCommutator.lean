/-
Copyright (c) 2021 Jordan Brown, Thomas Browning, Patrick Lutz. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Jordan Brown, Thomas Browning, Patrick Lutz
-/
import Mathbin.Data.Bracket
import Mathbin.GroupTheory.Subgroup.Basic
import Mathbin.Tactic.Group

/-!
# Commutators of Subgroups

If `G` is a group and `H₁ H₂ : subgroup G` then the commutator `⁅H₁, H₂⁆ : subgroup G`
is the subgroup of `G` generated by the commutators `h₁ * h₂ * h₁⁻¹ * h₂⁻¹`.

## Main definitions

* `⁅H₁, H₂⁆` : the commutator of the subgroups `H₁` and `H₂`.
-/


namespace Subgroup

variable {G G' : Type _} [Groupₓ G] [Groupₓ G'] {f : G →* G'}

/-- The commutator of two subgroups `H₁` and `H₂`. -/
instance commutator : HasBracket (Subgroup G) (Subgroup G) :=
  ⟨fun H₁ H₂ => closure { x | ∃ p ∈ H₁, ∃ q ∈ H₂, p * q * p⁻¹ * q⁻¹ = x }⟩

theorem commutator_def (H₁ H₂ : Subgroup G) : ⁅H₁,H₂⁆ = closure { x | ∃ p ∈ H₁, ∃ q ∈ H₂, p * q * p⁻¹ * q⁻¹ = x } :=
  rfl

instance commutator_normal (H₁ H₂ : Subgroup G) [h₁ : H₁.Normal] [h₂ : H₂.Normal] : Normal ⁅H₁,H₂⁆ := by
  let base : Set G := { x | ∃ p ∈ H₁, ∃ q ∈ H₂, p * q * p⁻¹ * q⁻¹ = x }
  suffices h_base : base = Groupₓ.ConjugatesOfSet base
  · dsimp only [commutator_def, ← base]
    rw [h_base]
    exact Subgroup.normal_closure_normal
    
  apply Set.Subset.antisymm Groupₓ.subset_conjugates_of_set
  intro a h
  simp_rw [Groupₓ.mem_conjugates_of_set_iff, is_conj_iff]  at h
  rcases h with ⟨b, ⟨c, hc, e, he, rfl⟩, d, rfl⟩
  exact
    ⟨d * c * d⁻¹, h₁.conj_mem c hc d, d * e * d⁻¹, h₂.conj_mem e he d, by
      group⟩

theorem commutator_mono {H₁ H₂ K₁ K₂ : Subgroup G} (h₁ : H₁ ≤ K₁) (h₂ : H₂ ≤ K₂) : ⁅H₁,H₂⁆ ≤ ⁅K₁,K₂⁆ := by
  apply closure_mono
  rintro x ⟨p, hp, q, hq, rfl⟩
  exact ⟨p, h₁ hp, q, h₂ hq, rfl⟩

theorem commutator_def' (H₁ H₂ : Subgroup G) [H₁.Normal] [H₂.Normal] :
    ⁅H₁,H₂⁆ = normalClosure { x | ∃ p ∈ H₁, ∃ q ∈ H₂, p * q * p⁻¹ * q⁻¹ = x } := by
  rw [← normal_closure_eq_self ⁅H₁,H₂⁆, commutator_def, normal_closure_closure_eq_normal_closure]

theorem commutator_le (H₁ H₂ : Subgroup G) (K : Subgroup G) :
    ⁅H₁,H₂⁆ ≤ K ↔ ∀, ∀ p ∈ H₁, ∀, ∀ q ∈ H₂, ∀, p * q * p⁻¹ * q⁻¹ ∈ K := by
  rw [Subgroup.commutator, closure_le]
  constructor
  · intro h p hp q hq
    exact h ⟨p, hp, q, hq, rfl⟩
    
  · rintro h x ⟨p, hp, q, hq, rfl⟩
    exact h p hp q hq
    

theorem commutator_containment (H₁ H₂ : Subgroup G) {p q : G} (hp : p ∈ H₁) (hq : q ∈ H₂) :
    p * q * p⁻¹ * q⁻¹ ∈ ⁅H₁,H₂⁆ :=
  (commutator_le H₁ H₂ ⁅H₁,H₂⁆).mp (le_reflₓ ⁅H₁,H₂⁆) p hp q hq

theorem commutator_comm (H₁ H₂ : Subgroup G) : ⁅H₁,H₂⁆ = ⁅H₂,H₁⁆ := by
  suffices ∀ H₁ H₂ : Subgroup G, ⁅H₁,H₂⁆ ≤ ⁅H₂,H₁⁆ by
    exact le_antisymmₓ (this _ _) (this _ _)
  intro H₁ H₂
  rw [commutator_le]
  intro p hp q hq
  have h : (p * q * p⁻¹ * q⁻¹)⁻¹ ∈ ⁅H₂,H₁⁆ :=
    subset_closure
      ⟨q, hq, p, hp, by
        group⟩
  convert inv_mem ⁅H₂,H₁⁆ h
  group

theorem commutator_le_right (H₁ H₂ : Subgroup G) [h : Normal H₂] : ⁅H₁,H₂⁆ ≤ H₂ := by
  rw [commutator_le]
  intro p hp q hq
  exact mul_mem H₂ (h.conj_mem q hq p) (inv_mem H₂ hq)

theorem commutator_le_left (H₁ H₂ : Subgroup G) [h : Normal H₁] : ⁅H₁,H₂⁆ ≤ H₁ := by
  rw [commutator_comm]
  exact commutator_le_right H₂ H₁

@[simp]
theorem commutator_bot (H : Subgroup G) : ⁅H,⊥⁆ = (⊥ : Subgroup G) := by
  rw [eq_bot_iff]
  exact commutator_le_right H ⊥

@[simp]
theorem bot_commutator (H : Subgroup G) : ⁅(⊥ : Subgroup G),H⁆ = (⊥ : Subgroup G) := by
  rw [eq_bot_iff]
  exact commutator_le_left ⊥ H

theorem commutator_le_inf (H₁ H₂ : Subgroup G) [Normal H₁] [Normal H₂] : ⁅H₁,H₂⁆ ≤ H₁⊓H₂ := by
  simp only [commutator_le_left, commutator_le_right, le_inf_iff, and_selfₓ]

theorem map_commutator {G₂ : Type _} [Groupₓ G₂] (f : G →* G₂) (H₁ H₂ : Subgroup G) :
    map f ⁅H₁,H₂⁆ = ⁅map f H₁,map f H₂⁆ := by
  apply le_antisymmₓ
  · rw [gc_map_comap, commutator_le]
    intro p hp q hq
    simp only [mem_comap, map_inv, map_mul]
    exact commutator_containment _ _ (mem_map_of_mem _ hp) (mem_map_of_mem _ hq)
    
  · rw [commutator_le]
    rintro _ ⟨p, hp, rfl⟩ _ ⟨q, hq, rfl⟩
    simp only [← map_inv, ← map_mul]
    exact mem_map_of_mem _ (commutator_containment _ _ hp hq)
    

theorem commutator_prod_prod {G₂ : Type _} [Groupₓ G₂] (H₁ K₁ : Subgroup G) (H₂ K₂ : Subgroup G₂) :
    ⁅H₁.Prod H₂,K₁.Prod K₂⁆ = ⁅H₁,K₁⁆.Prod ⁅H₂,K₂⁆ := by
  apply le_antisymmₓ
  · rw [commutator_le]
    rintro ⟨p₁, p₂⟩ ⟨hp₁, hp₂⟩ ⟨q₁, q₂⟩ ⟨hq₁, hq₂⟩
    exact ⟨commutator_containment _ _ hp₁ hq₁, commutator_containment _ _ hp₂ hq₂⟩
    
  · rw [prod_le_iff]
    constructor <;>
      · rw [map_commutator]
        apply commutator_mono <;>
          simp [le_prod_iff, map_map, MonoidHom.fst_comp_inl, MonoidHom.snd_comp_inl, MonoidHom.fst_comp_inr,
            MonoidHom.snd_comp_inr]
        
    

/-- The commutator of direct product is contained in the direct product of the commutators.

See `commutator_pi_pi_of_fintype` for equality given `fintype η`.
-/
theorem commutator_pi_pi_le {η : Type _} {Gs : η → Type _} [∀ i, Groupₓ (Gs i)] (H K : ∀ i, Subgroup (Gs i)) :
    ⁅Subgroup.pi Set.Univ H,Subgroup.pi Set.Univ K⁆ ≤ Subgroup.pi Set.Univ fun i => ⁅H i,K i⁆ :=
  (commutator_le _ _ _).mpr fun p hp q hq i hi => commutator_containment _ _ (hp i hi) (hq i hi)

/-- The commutator of a finite direct product is contained in the direct product of the commutators.
-/
theorem commutator_pi_pi_of_fintype {η : Type _} [Fintype η] {Gs : η → Type _} [∀ i, Groupₓ (Gs i)]
    (H K : ∀ i, Subgroup (Gs i)) :
    ⁅Subgroup.pi Set.Univ H,Subgroup.pi Set.Univ K⁆ = Subgroup.pi Set.Univ fun i => ⁅H i,K i⁆ := by
  classical
  apply le_antisymmₓ (commutator_pi_pi_le H K)
  · rw [pi_le_iff]
    intro i hi
    rw [map_commutator]
    apply commutator_mono <;>
      · rw [le_pi_iff]
        intro j hj
        rintro _ ⟨_, ⟨x, hx, rfl⟩, rfl⟩
        by_cases' h : j = i
        · subst h
          simpa using hx
          
        · simp [h, one_mem]
          
        
    

end Subgroup

